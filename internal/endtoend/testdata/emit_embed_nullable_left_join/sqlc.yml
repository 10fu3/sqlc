version: "2"
sql:
  - engine: "postgresql"
    queries: 
      - "query.sql"
      - "query_multiple.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        emit_embed_nullable_left_join: true